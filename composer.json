{
    "name": "JSKOS-API Wrappers",
    "require": {
        "gbv/jskos": "^0.0.8",
        "easyrdf/easyrdf": "^0.9.1"
    },
    "require-dev": {
        "phpunit/phpunit": "~4.8"
    },
    "scripts": {
        "test": [
            "vendor/bin/phpunit tests"
        ],
        "travis-test": [
            "vendor/bin/phpunit tests --coverage-clover build/logs/clover.xml"
        ]
    }
}
